<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Desafio53" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <!-- Menú de navegación -->
    <nav style="background-color: #333; padding: 10px;">
      <ul style="list-style: none; display: flex; justify-content: center; padding: 0; margin: 0;">
        <li style="margin: 0 15px;">
          <%= link_to 'Inicio', root_path, style: 'color: white; text-decoration: none; font-size: 18px;' %>
        </li>
        <li style="margin: 0 15px;">
          <%= link_to 'Películas', movies_path, style: 'color: white; text-decoration: none; font-size: 18px;' %>
        </li>
        <li style="margin: 0 15px;">
          <%= link_to 'Series', series_path, style: 'color: white; text-decoration: none; font-size: 18px;' %>
        </li>
        <li style="margin: 0 15px;">
          <%= link_to 'Documentales', documentaries_path, style: 'color: white; text-decoration: none; font-size: 18px;' %>
        </li>
      </ul>
    </nav>

    <!-- Contenido principal de la página -->
    <%= yield %>
  </body>
</html>

